app:
  services:
    orders: http://tap-otel-orders:8080
    items: http://tap-otel-items:8080

server:
  port: ${PORT:8080}

management:
  metrics:
    export:
      wavefront:
        uri: http://wavefront-proxy.wavefront.svc.cluster.local:2878
